% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_mcod_data.R
\name{.import_mcod_data}
\alias{.import_mcod_data}
\title{Shared engine for importing restricted/public MCOD fixed-width data}
\usage{
.import_mcod_data(
  file,
  year_x,
  tier = c("restricted", "public"),
  dict = NULL,
  restricted_dict = NULL
)
}
\arguments{
\item{file}{path to the raw fixed-width file}

\item{year_x}{year of MCOD data}

\item{tier}{\code{"restricted"} or \code{"public"}}

\item{dict}{optional dictionary to use instead of the packaged one (for tests)}

\item{restricted_dict}{optional restricted dictionary defining the canonical
column order (for tests); defaults to the packaged \code{mcod_fwf_dicts}}
}
\value{
a tibble
}
\description{
Selects the byte-verified dictionary for \code{year_x} and \code{tier} (by
bare name, resolving to the internal copy in \code{R/sysdata.rda}), builds the
\code{readr::fwf_positions()} and the \code{col_types} string from the SAME
rows in stored order (so they stay aligned, including the intentional
nested/overlapping geography fields), reads the file, appends any suppressed
columns as typed all-\code{NA}, and reorders to the canonical restricted
column order for column parity across tiers.
}
\keyword{internal}
