% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-pop_singlerace_full.R
\docType{data}
\name{pop_singlerace_full}
\alias{pop_singlerace_full}
\title{Single-race population estimates, national, 2000-2024 (backfill)}
\format{
A data frame with 10800 rows and 9 columns
\describe{
  \item{year}{year of observation (2000-2024)}
  \item{age}{starting age for the 5-year age bin (0, 5, ..., 85 = 85+)}
  \item{sex}{\code{"male"} or \code{"female"}}
  \item{race}{single-race group: \code{white_only}, \code{black_only},
    \code{american_indian_only}, \code{asian_only}, \code{nhopi_only},
    \code{multiracial}}
  \item{hispanic_origin}{\code{"non_hispanic"} or \code{"hispanic"}}
  \item{pop}{population count}
  \item{scheme}{race scheme (\code{"single"})}
  \item{source}{data source (\code{"census_pep"})}
  \item{vintage}{PEP vintage for that row: \code{"int2000"}, \code{"int2010"},
    or \code{"V2024"}}
}
}
\source{
\url{https://www2.census.gov/programs-surveys/popest/datasets/}
}
\usage{
pop_singlerace_full
}
\description{
Annual US resident population by age (5-year bins, 18 groups), sex, single-race
group (the six OMB-1997 alone categories), and Hispanic origin, from the Census
Bureau Population Estimates Program (PEP). This is the 2000-2024 BACKFILL of the
single-race series: it extends \code{\link{pop_singlerace}} (2020-2024 only)
back to 2000 by stitching three PEP vintages, each contributing a disjoint,
non-overlapping calendar-year range.
}
\details{
Use it with single-race death counts (\code{remap_race()}/\code{categorize_race()}
codes 101-106, coded 2020 and later). It is a single, consistent taxonomy across
all 25 years (RACE 1-6), NOT comparable to the bridged \code{\link{pop_bridged}}
or the legacy single-race-alone \code{\link{pop_est}}. The 2020-2024 slice is
identical (to the person) to the frozen \code{\link{pop_singlerace}}. Only the
finest cells are stored; \code{"total"}/\code{"both"}/\code{"all"} are
synthesized on demand, never stored, so aggregation never double-counts.

The \code{vintage} column records each row's provenance:
\code{"int2000"} (2000-2009, the 2000-2010 intercensal estimates),
\code{"int2010"} (2010-2019, the rebased 2010-2020 intercensal estimates -- NOT
the postcensal V2020 series, which was never re-based to the 2020 census), and
\code{"V2024"} (2020-2024, the Vintage-2024 estimates).

State- and county-level single-race backfill denominators are distributed
separately (too large to bundle) as \code{pop_singlerace_state_full} /
\code{pop_singlerace_county_full} and fetched via
\code{download_pop_data(scheme = "single")} / \code{get_pop_county()}.
}
\keyword{datasets}
