% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_methadone_present.R
\name{flag_methadone_present}
\alias{flag_methadone_present}
\title{Creates a column `methadone_present` if opioid death involved methadone}
\usage{
flag_methadone_present(
  processed_df,
  year = NULL,
  keep_cols = FALSE,
  opioid_deaths_only = TRUE
)
}
\arguments{
\item{processed_df}{MCOD dataframe already processed}

\item{year}{if NULL, will attempt to detect}

\item{keep_cols}{keep intermediate columns}

\item{opioid_deaths_only}{if `TRUE` (default) the flag fires only on opioid
deaths (`opioid_death == 1`); if `FALSE`, it fires wherever the methadone
code appears (including contributory-only records) and the caller is
expected to `filter(opioid_death == 1)` themselves.}
}
\value{
a new dataframe with 1 additional column
}
\description{
This function flags all opioid deaths that involved methadone.
}
\examples{
df <- data.frame(year = 2019, ucod = "X42", f_records_all = "T403")
df |>
    flag_opioid_deaths(year = 2019) |>
    flag_methadone_present(year = 2019)
}
